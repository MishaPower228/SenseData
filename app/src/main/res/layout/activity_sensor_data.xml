<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_color">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/rounded_toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"

        app:navigationIcon="@drawable/baseline_arrow_back_24"
        app:navigationIconTint="@color/weather_card_text"
        app:titleCentered="true"
        app:titleTextColor="@color/weather_card_text"/>

    <ProgressBar
        android:id="@+id/progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="48dp"
        android:layout_height="48dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- по одному TextView на показник -->
            <TextView
                android:id="@+id/tvTemp"
                style="@style/SensorPill"
                android:text="🌡 Температура: …"/>
            <TextView
                android:id="@+id/tvHumi"
                style="@style/SensorPill"
                android:text="💧 Вологість: …"/>
            <TextView
                android:id="@+id/tvPressure"
                style="@style/SensorPill"
                android:text="🧭 Тиск: …"/>
            <TextView
                android:id="@+id/tvAltitude"
                style="@style/SensorPill"
                android:text="⛰ Висота: …"/>
            <TextView
                android:id="@+id/tvTempBme"
                style="@style/SensorPill"
                android:text="🌡 T(BME): …"/>
            <TextView
                android:id="@+id/tvHumiBme"
                style="@style/SensorPill"
                android:text="💧 H(BME): …"/>
            <TextView
                android:id="@+id/tvGas"
                style="@style/SensorPill"
                android:text="🔥 Газ: …" />
            <TextView
                android:id="@+id/tvLight"
                style="@style/SensorPill"
                android:text="💡 Світло: …"/>
            <TextView
                android:id="@+id/tvMq2"
                style="@style/SensorPill"
                android:text="🧪 MQ2: …"/>
            <TextView
                android:id="@+id/tvMq2P"
                style="@style/SensorPill"
                android:text="🧪 MQ2, %: …"/>
            <TextView
                android:id="@+id/tvLightA"
                style="@style/SensorPill"
                android:text="🔆 Освітленість (A): …"/>
            <TextView
                android:id="@+id/tvLightAP"
                style="@style/SensorPill"
                android:text="🔆 Освітленість, %: …"/>
        </LinearLayout>
    </ScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
