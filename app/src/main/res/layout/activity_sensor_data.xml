<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_toolbar_color">

    <!-- 🔹 Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/rounded_toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:navigationIcon="@drawable/baseline_arrow_back_24"
        app:navigationIconTint="@color/weather_card_text">

        <!-- 🔹 Тепер заголовок окремим TextView -->
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Назва кімнати"
            android:textColor="@color/weather_card_text"
            android:textSize="18sp"
            android:textStyle="bold"/>
    </com.google.android.material.appbar.MaterialToolbar>

    <!-- 🔹 Прогресбар -->
    <FrameLayout
        android:id="@+id/progressOverlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/toolbar"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />
    </FrameLayout>
    <!-- 🔹 SwipeRefreshLayout -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/swipeRefreshSensor"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- 🔹 Основні -->
                <TextView android:id="@+id/tvTemp"     style="@style/SensorPill" android:text="🌡 Температура: …"/>
                <TextView android:id="@+id/tvHumi"     style="@style/SensorPill" android:text="💧 Вологість: …"/>
                <TextView android:id="@+id/tvPressure" style="@style/SensorPill" android:text="🧭 Тиск: …"/>
                <TextView android:id="@+id/tvAltitude" style="@style/SensorPill" android:text="⛰ Висота: …"/>
                <TextView android:id="@+id/tvTempBme"  style="@style/SensorPill" android:text="🌡 T(BME): …"/>
                <TextView android:id="@+id/tvHumiBme"  style="@style/SensorPill" android:text="💧 H(BME): …"/>

                <!-- 🔹 Газ -->
                <!-- Газ -->
                <TextView
                    android:id="@+id/tvGas"
                    style="@style/SensorPill"
                    android:text="🔥 Газ (детект): …" />

                <TextView
                    android:id="@+id/tvGasAnalog"
                    style="@style/SensorPill"
                    android:text="🔥 Газ (аналог, %): …" />

                <!-- Світло -->
                <TextView
                    android:id="@+id/tvLight"
                    style="@style/SensorPill"
                    android:text="💡 Світло (детект): …" />

                <TextView
                    android:id="@+id/tvLightAnalog"
                    style="@style/SensorPill"
                    android:text="💡 Світло (аналог, %): …" />

                <!-- 🔹 Divider -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="8dp"
                    android:layout_marginTop="8dp"
                    android:background="@android:color/transparent" />

                <!-- 🔹 Рекомендації -->
                <TextView
                    android:id="@+id/tvAdviceTitle"
                    style="@style/SensorPill"
                    android:text="🧠 Рекомендації"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/tvAdvice"
                    style="@style/SensorPill"
                    android:text="—"
                    android:maxLines="6"
                    android:ellipsize="end" />

            </LinearLayout>
        </ScrollView>
    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
